# ===== CONFIGURACIÓN DE LA APP MÓVIL =====
# IMPORTANTE: Las variables DEBEN tener el prefijo EXPO_PUBLIC_ para ser accesibles en la app

# URL completa del servidor backend (donde corre server.js)
# Formato: http://TU_IP_LOCAL:PUERTO
# La IP debe ser la misma donde está corriendo el backend (por defecto puerto 3000)
# Para obtener tu IP local:
#   - Windows: Abre CMD y ejecuta "ipconfig" (busca IPv4 Address)
#   - Linux/Mac: Ejecuta "ifconfig" o "ip addr show"

# Ejemplo: http://192.168.1.100:3000
EXPO_PUBLIC_API_BASE_URL=http://TU_IP_AQUI:3000

# IP del servidor donde está instalado Moodle (solo la IP, sin http:// ni puerto)
# Debe ser accesible desde tu dispositivo móvil/emulador
# Ejemplo: 192.168.1.100
EXPO_PUBLIC_MOODLE_IP=TU_IP_AQUI
